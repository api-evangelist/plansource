generated: '2026-08-06'
method: derived
source:
- mcp/plansource-mcp.yml
- openapi/plansource-admin-api-openapi-original.json
surfaces:
  openapi:
    file: openapi/plansource-admin-api-openapi-original.json
    title: Plansource Admin API
    version: 2.0.0
    server: https://api.plansource.com/admin/v2
    operations: 80
    gated: false
  mcp:
    url: https://developer.plansource.com/mcp
    gated: true
    gate: OAuth 2.0 (RFC 9728 protected resource); anonymous tools/list returns -32001 Authorization required
    kind: documentation MCP (ReadMe platform) - retrieval over the developer portal, not an execution
      surface over the Admin API
  graphql: null
crosswalk: []
crosswalk_note: Empty by design, not by omission. The MCP tool list is OAuth-gated, so no tool name was
  observed; binding unobserved tools to operationIds would be fabrication. Additionally the MCP server
  is a ReadMe documentation server whose tools retrieve docs pages, so a tool-to-operationId binding is
  not expected to exist even once the gate is opened.
mcp_only: []
rest_only:
- capability: ACA
  operations:
  - GET /aca/enrollees
  - GET /aca/enrollees/subscriber/{subscriber_id}
  - GET /aca/offers
  - GET /aca/offers/subscriber/{subscriber_id}
- capability: Administrators
  operations:
  - GET /administrator/{id}
  - GET /administrators
  - POST /administrator
  - PUT /administrator/{id}
  - PUT /administrator/{id}/disable
- capability: Coverage
  operations:
  - GET /coverage/composites
  - GET /coverage/dependent/{dependent_coverage_id}
  - GET /coverage/line/{coverage_line_id}
  - GET /coverage/meta
  - GET /coverage/subscriber/{subscriber_id}
  - GET /coverage/subscriber/{subscriber_id}/composite
  - GET /coverage/{coverage_id}
  - GET /coverage/{coverage_id}/composite
  - GET /coverage/{coverage_id}/dependents
  - GET /coverage/{coverage_id}/lines
  - GET /coverages
  - GET /coverages/dependents
  - GET /coverages/lines
  - POST /coverage
  - POST /coverage/{coverage_id}/dependent
  - POST /coverage/{coverage_id}/line
  - POST /coverages
  - POST /coverages/dependents
  - POST /coverages/lines
  - PUT /coverage/dependent/{dependent_coverage_id}
  - PUT /coverage/dependent/{dependent_coverage_id}/terminate
  - PUT /coverage/line/{coverage_line_id}
  - PUT /coverage/line/{coverage_line_id}/terminate
  - PUT /coverage/subscriber/{subscriber_id}/terminate
  - PUT /coverage/{coverage_id}
  - PUT /coverage/{coverage_id}/terminate
  - PUT /coverages
  - PUT /coverages/dependents
  - PUT /coverages/dependents/terminate
  - PUT /coverages/lines
  - PUT /coverages/lines/terminate
  - PUT /coverages/terminate
- capability: Demographic
  operations:
  - DELETE /coverage/{coverage_id}/beneficiaries
  - GET /beneficiaries
  - GET /beneficiaries/meta
  - GET /coverage/{coverage_id}/beneficiaries
  - GET /dependent/meta
  - GET /dependents
  - GET /subscriber/meta
  - GET /subscriber/{id}
  - GET /subscriber/{subscriber_id}/beneficiaries
  - GET /subscriber/{subscriber_id}/dependent/{id}
  - GET /subscribers
  - POST /coverage/{coverage_id}/beneficiaries
  - POST /subscriber
  - POST /subscriber/{subscriber_id}/dependent
  - POST /subscribers
  - PUT /coverage/{coverage_id}/beneficiaries
  - PUT /subscriber/{id}
  - PUT /subscriber/{id}/rehire
  - PUT /subscriber/{id}/terminate
  - PUT /subscriber/{subscriber_id}/dependent/{id}
  - PUT /subscriber/{subscriber_id}/dependent/{id}/deactivate
  - PUT /subscriber/{subscriber_id}/dependent/{id}/reactivate
  - PUT /subscribers
  - PUT /subscribers/rehire
  - PUT /subscribers/terminate
- capability: EOI
  operations:
  - GET /eoi/subscriber/{subscriber_id}
  - GET /eoi/subscribers
  - PUT /eoi/decision/{subscriber_id}
  - PUT /eoi/decisions
  - PUT /eoi/form_completion/{subscriber_id}
  - PUT /eoi/form_completions
- capability: Page Content
  operations:
  - GET /content_blocks
  - GET /page_content
  - PUT /content_block
  - PUT /page_content
- capability: Payroll
  operations:
  - GET /payroll_coverages_subscriber/{id}
  - GET /payroll_coverages_subscribers
- capability: Security
  operations:
  - DELETE /user_sessions
- capability: Untagged
  operations:
  - GET /organizations
coverage:
  tools_named: 0
  tools_bound: 0
  mcp_only: 0
  rest_operations_total: 80
  rest_operations_with_tool: 0
  note: 0/80 REST operations have a known MCP tool. The Admin API has no agent-executable MCP projection
    today; the only MCP surface is documentation retrieval.
spec_note: The published OpenAPI declares no operationId on any of its 80 operations, so REST operations
  are identified here by METHOD + path, which is what the spec actually provides. ReadMe's reference slugs
  (get_subscribers, post_subscriber, ...) are generated by the docs platform, not by the provider's spec.
